feedparser
requests
trafilatura
python-dateutil
pyyaml
mysql-connector-python



Input: RSS feed URL
Step A: Parse RSS to obtain a list of article URLs
Step B: Download article HTML (with user agent, timeout, retry, and speed limit)
Step C: Extract main text from HTML (body, title, author/time if available)
Step D: Clean and normalize (remove extra spaces, apply minimum length filter)
Step E: Store in database (SQLite: sources, documents, fetch_log)
Output: Text content in the database that can be retrieved (for your subsequent use in inverted index/BM25)

Add a new source: In main.py, replace the feed_url, or put multiple feeds into a list and loop through them.
Add database: Replace crawler/output.py with db_writer.py, change write_jsonl to write_sql(...) and keep pipeline unchanged.
Add site rules: In extractor.py, add "domain-specific strategy" (for example, first try CSS selectors for certain domains, and then fall back to trafilatura).
Additional comment: Replace the for loop in pipeline.py with a thread pool/asyncio loop, but make sure that the rate limiting and failure retry logic are not compromised.