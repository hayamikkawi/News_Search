[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "common-utils"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["common_utils"]
