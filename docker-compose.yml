services:
  ttds-db:
    image: mysql:8.0
    container_name: ttds_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ttds#123
    volumes:
      - ./mysql-data:/var/lib/mysql
      - ./scripts:/docker-entrypoint-initdb.d
    ports:
      - "3306:3306"
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - ttds-network

  ttds-frontend:
    image: nginx:alpine
    container_name: ttds_frontend
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./frontend/GlobalSearch.html:/usr/share/nginx/html/GlobalSearch.html:ro
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - ttds-db
    networks:
      - ttds-network

networks:
  ttds-network:
    driver: bridge


### !!! ToDo: Configure and enable the following services when ready !!!
# # Crawler Service
#   ttds-crawler:
#     build: ./code/crawler  # Point to the crawler code directory
#     container_name: ttds_crawler
#     depends_on:
#       - ttds-db            # Ensure the database starts before the crawler
#     environment:
#       - DB_HOST=ttds-db    # In Docker network, use service name to connect
#       - DB_USER=ttds_app
#       - DB_PASS=app_password_2026
#     volumes:
#       - ./code/crawler:/app # Mount code for hot reload

#   # Indexer Service
#   ttds-indexer:
#     build: ./code/indexer
#     container_name: ttds_indexer
#     depends_on:
#       - ttds-db
#     volumes:
#       - ./mysql-data:/data/raw     # Read raw database data mount point
#       - ./index-storage:/app/index # ！！！Important: Persist the built index to disk